#include <iostream>
#include <vector>

using namespace std;

// Recalcular a nova rota
int recalcular(){
    // Subtrair o valor do custo anterior e posterior do vertice atual
    // Somar o valor do custo anterior e posterior do vertice novo
    return;
}

// Realizar a troca de clientes em uma msm rota
vector<int> swap(){
    // Trocar cliente x por y (x!=y e x!=0 e y!=0)
    return ;
}

vector<int> buscaExaustiva(){
    // Um loop de quantidades de trocas possíveis:
        // Chamar a função swap
        // Comparar com a anterior e caso tenha melhorada
        // A nova rota é retornada
    return; 
}